<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EDIT</title>
</head>

<body>
    <h1>EDIT a element</h1>
      <!--<form action="/sample" method="post">this line only for edit> -->
    <form action="/sample/<%=edit._id%>?_method=put" method="post">
        <label for="name">New element :</label>
        <input type="text" id="name" name="name" value="<%= edit.name %> ">

        <label for="price">Price in Rs:</label>
        <input type="number" id="price" name="price" value="<%= edit.price%>">

        <label for="category">Select the category</label>
        <select name="category" id="category">
            <option value="fruit"<%= edit.category === 'fruit' ? 'selected' : '' %> >Fruit</option>
            <option value="vegetable" <%= edit.category === 'vegetable' ? 'selected' : '' %>>Vegetable</option>
            <option value="diary" <%= edit.category === 'diary' ? 'selected' : '' %>>Diary</option>
            <option value="dryfood" <%= edit.category === 'dryfood' ? 'selected' : '' %>>Dry Food</option>
        </select>
        <button>SUBMIT</button>
    </form>
    <a href="/sample/<%= edit._id %> ">Cancel</a>
    
</body>
</html>